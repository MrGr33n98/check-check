<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #2196F3; color: white; padding: 20px; text-align: center; }
      .content { padding: 20px; background-color: #f9f9f9; }
      .footer { padding: 20px; text-align: center; color: #666; }
      .button { display: inline-block; padding: 10px 20px; background-color: #4CAF50; color: white; text-decoration: none; border-radius: 5px; }
      .info-box { background-color: #e3f2fd; border: 1px solid #bbdefb; padding: 15px; border-radius: 5px; margin: 20px 0; }
      .provider-details { background-color: white; padding: 15px; border-radius: 5px; margin: 15px 0; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>🏢 Nova Empresa Cadastrada</h1>
      </div>
      
      <div class="content">
        <h2>Nova solicitação de cadastro</h2>
        <p>Uma nova empresa foi cadastrada na plataforma e aguarda aprovação.</p>
        
        <div class="provider-details">
          <h3>📋 Detalhes da Empresa</h3>
          <ul>
            <li><strong>Nome:</strong> <%= @provider.name %></li>
            <li><strong>Título:</strong> <%= @provider.title %></li>
            <li><strong>País:</strong> <%= @provider.country %></li>
            <li><strong>Ano de Fundação:</strong> <%= @provider.foundation_year %></li>
            <li><strong>Número de Funcionários:</strong> <%= @provider.members_count %></li>
            <li><strong>Status:</strong> <%= @provider.status_label %></li>
            <li><strong>Cadastrado em:</strong> <%= @provider.created_at.strftime('%d/%m/%Y às %H:%M') %></li>
          </ul>
          
          <% if @provider.short_description.present? %>
            <p><strong>Descrição:</strong></p>
            <p><%= @provider.short_description %></p>
          <% end %>
          
          <% if @provider.tags.any? %>
            <p><strong>Tags:</strong> <%= @provider.tags.join(', ') %></p>
          <% end %>
          
          <% if @provider.social_links.any? %>
            <p><strong>Links Sociais:</strong></p>
            <ul>
              <% @provider.social_links.each do |link| %>
                <li><a href="<%= link %>" target="_blank"><%= link %></a></li>
              <% end %>
            </ul>
          <% end %>
        </div>
        
        <div class="info-box">
          <h3>⏰ Ação Necessária</h3>
          <p>Esta empresa precisa ser analisada e aprovada/rejeitada por um administrador.</p>
          <p>Acesse o painel administrativo para revisar os detalhes completos e tomar uma decisão.</p>
        </div>
        
        <p style="text-align: center; margin: 30px 0;">
          <a href="<%= Rails.application.routes.url_helpers.admin_root_url %>/providers/<%= @provider.id %>" class="button">Revisar no Admin</a>
        </p>
      </div>
      
      <div class="footer">
        <p>Sistema Check Check Solar<br>Notificação Automática</p>
        <p><small>Este é um email automático do sistema administrativo.</small></p>
      </div>
    </div>
  </body>
</html>